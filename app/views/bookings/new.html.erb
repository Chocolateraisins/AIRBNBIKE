<div class="container">
  <h1>Book this bike now</h1>
  <%= simple_form_for [@bike, @booking] do |f| %>
    <%= f.input :start_date, as: :date, html5: true %>
    <%= f.input :end_date, as: :date, html5: true %>
    <%= f.input :delivery_address %>
    <%= f.submit "book this bike", class: "btn btn-primary" %>
  <% end %>
</div>
